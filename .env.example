# Copy this file to .env and fill in your values.
# The capture.py script reads these env vars so you don't have to pass flags
# every time. Alternatively, just use the CLI flags directly.

# Blink account credentials (used on first login; omit after credentials are cached)
BLINK_USERNAME=your@email.com
BLINK_PASSWORD=yourpassword

# Capture interval in seconds (default: 600 = 10 minutes)
BLINK_INTERVAL=600

# Where to store captured JPEG frames (default: ./frames)
BLINK_FRAMES_DIR=frames

# Where to cache auth credentials between runs (default: .credentials.json)
BLINK_CREDENTIALS=.credentials.json
